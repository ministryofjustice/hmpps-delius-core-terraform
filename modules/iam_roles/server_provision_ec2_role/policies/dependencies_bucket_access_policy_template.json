{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Action": [
        "s3:Get*",
        "s3:List*"
      ],
      "Effect": "Allow",
      "Resource": [
        "${dependencies_bucket_arn}",
        "${dependencies_bucket_arn}/*"
      ]
    }
  ]
}
