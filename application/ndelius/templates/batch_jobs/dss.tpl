{
    "image": "${job_image}",
    "jobRoleArn": "${job_role}",
    "memory": ${job_memory},
    "vcpus": ${job_vcpus},
    "environment": [
        ${job_envvars}
    ],
    "ulimits": [
        ${job_ulimits}
    ],
}